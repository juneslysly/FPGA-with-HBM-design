\documentclass{article}
\usepackage[english]{babel}
\usepackage{booktabs} 
\usepackage[letterpaper,top=2cm,bottom=2cm,left=3cm,right=3cm,marginparwidth=1.75cm]{geometry}
 
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage[colorlinks=true, allcolors=blue]{hyperref}
\usepackage[linesnumbered, ruled]{algorithm2e}
\usepackage{rotfloat}
%----------------------------------------------
\newcommand{\nomunit}[1]{%
\renewcommand{\nomentryend}{\hspace*{\fill}#1}}
%----------------------------------------------
\SetKwRepeat{Do}{do}{while}%

\title{
	\textbf{
      Accelerate MD Simulations by FPGA with HBM design
    }} 
\author{1,1,1,1,1,1}
\date{2024/10/23}

\begin{document}
\maketitle

%
% abstract
%
\begin{abstract}

\noindent
Molecular dynamics (MD) simulation is a powerful tool that models the behavior of particles over a given time interval, complementing biological and chemical experimental methods, particularly in materials science and drug discovery.
The speed of MD simulations is one of the key challenges that prevent their applications. 
To address this, a full hardware module based on field programmable gate arrays (FPGAs) has been designed to accelerate all parts of MD simulations. 
The solution aims to increase computational speed by utilizing pipeline techniques (Pipeline) to optimize the allocation of individual tasks within the entire parallel system.
We have also explored the impact of high-bandwidth memory (HBM2) on computational performance, comparing it with existing memory structures.
The work includes the use of DDR4 high-speed interfaces and the implementation of polling algorithms to improve access efficiency.
The optimized design of the underlying register-transfer level (RTL) circuitry for a single node has fundamentally enhanced the efficiency of data read and write operations to and from registers, with external storage read and write efficiency improved by more than 2.0 times.
The implementation of our work was demonstrated by comparing the simulation results of noble gas argon and dihydrofolate reductase (DHFR) with those obtained from OpenMM.
Additionally, potential strategies for continuing to improve the efficiency of MD simulations are also discussed.

\end{abstract}

\section{Introduction}

\noindent
MD simulations have proven to provide critical insights into the properties and interactions of materials and biological systems, making them an important tool in materials science and drug discovery, and showing great promise in these fields~\cite{fep_runtong,fep_york,md_in_dd_jmc2016,md_in_material1}. 
However, as the reserrch interest grows in increasingly complex and larger-scale systems, the speed of MD simulations remains a significant challenge, directly impacting the efficiency of exploring their potential applications. 
To address this, almost all existing MD programs have developed multi-CPUs parallel strategies and GPU-based codes to accelerate simulations, significantly enhancing computational performance~\cite{charmm_at_c45,amber_gpu1,amber_gpu2,namd_gpu,omm7,gmx}. \\

\noindent
In addition to GPU and multi-core CPU approaches, several research groups around the world use dedicated computing hardware to accelerate MD simulations. 
The commercial ANTON supercomputers (Generations I to III) all utilize the application specific integrated circuit (ASIC) process and have up to 512 nodes of dedicated clusters~\cite{anton1,anton2,anton3}.
However, the ANTON series of supercomputers are expensive to design, have long commissioning and development cycles, and can not easily access. 
Additionally, the MD-GRAPE series, which dates back to the late 1980s, uses MCUs, CPLDs (Complex Programmable Logic Devices), FPGAs, and other acceleration circuits to build a multi-node design~\cite{MD-GRAPE-3,MDGRAPE-4A}.
This design is combined with a specialized fiber-optic transmission network to form a large-scale computational cluster. 
The MD-GRAPE series has been developed through four generations, and its hardware devices are constantly updated and iterated, but the design cost remains very high. 
Furthermore, the Novo-G series, which uses commercial FPGA accelerator cards from the Intel series, optimizes molecular dynamics computational efficiency from both hardware and software perspectives~\cite{Novo-G,Novo-G2}.\\
% add comment for the Novo-G

% MD difficults
% current available hardwares
% our attempts
\noindent
%To accelerate MD simulations using hardwares, 
Currently available hardware design platforms include FPGAs and ASICs , which are process-cured logic circuits, and FPGAs are integrated chips designed for commercial applications.
Additional, existing FPGA manufacturing processes are capable of achieving nanometer sizes of 3 nm or less\cite{ref3,ref18}. 
Although FPGAs are less dense and more slower than ASICs, they are more flexible in design, more cost-effective, and more economical in terms of power consumption compared to other devices\cite{ref3}.
FPGAs incorporate a variety of unique production techniques, such as the FLASH process and the reverse fusion process, which allow them to support multiple recasts and debugs in the commercial sector to accommodate different experimental design needs\cite{ref22}. 
In the chip design phase, FPGA design is regarded as the core of prototyping, which also means that FPGA circuit design has great potential for practical application in the later stage of curing into an ASIC process\cite{ref3,ref18}. 
An important step in the MD simulation process is the updating of the system state at each moment.
This means that the forces on all particles in the system are calculated at one time step, and then the position and velocity of each particle at the next time step are calculated by an integrator\cite{ref22}.
This cycle completes the simulation. 
The calculation of the forces on the particles is crucial to the accuracy of the MD results, and the calculation of the inter-particle interactions is the decisive speed step in the program\cite{ref6}.\\

\noindent
%Different impact of memories on the overall design.
A full hardware module is designed to accelerate all parts of the MD computation, which is aimed at increasing the speed of computation, and utilizes the pipeline technique to optimize the allocation of tasks in the whole parallel system\cite{ref12,ref23}. 
A single instruction multiple data stream (SIMD) approach is utilized to significantly improve the computational efficiency. At the same time, we also built a software framework dedicated to this scheme, which can be used to accelerate the data processing of the circuit. 
To better exploit the parallelism potential, we comprehensively compared existing high-speed interfaces and memory devices, and analyzed existing high-speed architectural designs\cite{ref18,ref21}.
The new generation of high-bandwidth memories, such as HBM2 memories, are used to fully compare the impact of existing memory structures on computational performance in MD design. 
A great deal of work has been done using DDR4 high-speed interfaces and polling algorithms to improve access efficiency\cite{ref19,ref20}.
The new generation of high-speed, high-bandwidth, multi-parallel interface HBM memory is adopted to enable read/write access to external mass storage while parallelizing the computation of multiple data paths\cite{ref6}.\\

\noindent
%Designed in RTL language or in HLS language? 
The optimized design of the underlying RTL circuit fundamentally improves the efficiency of reading and writing registers. The optimized design of the underlying RTL circuit fundamentally improves the efficiency of data reading and writing registers, and more than doubles the efficiency of reading and writing external storage\cite{ref3,ref19}. Therefore, the rational use of external mass storage devices is fully considered in the design process. We select the same Xilinx UV9P chip of the cloud platform, and use two kinds of more efficient memories, DDR4 and HBM2, because there are a large number of intermediate computational data in MD computation, which are directly related to the particle information, so the storage data need independent storage intervals, and the DDR is a single read/write channel, and the HBM supports 32 independent read/write channels, which greatly increases the read/write storage space of particle computation, and the HBM supports 32 independent read/write channels\cite{ref19,ref20}. 
DDR is a single read/write channel, while HBM supports 32 independent read/write channels, which greatly improves the data throughput efficiency for particle computation\cite{ref18,ref22}.
The use of multi-channel computing improves the efficiency of reading and writing to external memory, but the consumption of on-chip resources by the read/write driver needs to be fully considered\cite{ref20,ref21,ref22}. We use the progress checking module to supervise the multi-channel data to ensure the orderly reading and writing of data\cite{ref20,ref21,ref22,ref23}.\\

\noindent
% this should not in Introdution
%The Influence of Numerical Analysis Method on Calculation Speed.
% For the same MD simulation system, the data is "sliced" according to the computational node and then sent to the specified computational unit. 
% The computational unit needs to use a reasonable floating-point computational model. 
% In this design, we compare various numerical models and model the first, second, and third-order computational models using multistage linear interpolation to create a numerical interpolation table by slicing each fitted curve rationally. 
% The interpolation table is transferred to the on-chip memory via the high-speed AXI Lite protocol, thus ensuring high computational efficiency. 
% Meanwhile, HLS advanced compilation and RTL design are used to finalize the RTL design circuit to ensure computational efficiency. 
% The interpolation algorithm is compared with Newtonian interpolation, Hamiltonian interpolation and Legendre interpolation\cite{ref3}. 
% After the same MD simulation system is divided into several blocks, a reasonable proximity force calculation supervision module needs to be designed, which needs to record the computational progress of the data in each block. 
% Assuming that a simulation system is divided into 1000 or more sub units and that the upper and lower hemispheres are computed separately, the design of the supervision module would be difficult. We use a multilevel supervision approach to ensure that the data in each sub unit is accurate during the computation.\\


\section{Theoretical background}

\subsection{Classic force field???}

\noindent
MD program can divided into four parts.
First, simulated system should read in the specified operation conditions, such as initial temperature, particle number, unit time step, etc\cite{ref1}.
Second, initialization speed, determine the initial position and speed of particles.
Third, Calculate the forces acting on the particles.
Finally, Solving the Newton's equation of motion.
Repeat steps 3 and 4 until the specified simulation step is met.\\


\begin{equation}
\boldsymbol{F}^{\text {total}} = \boldsymbol{F}^{\text {bonded}} 
                                + \boldsymbol{F}^{\text{non-bonded}}
\end{equation}

where the bonded force consists of bond, angle, dihedral and improper terms:

\begin{equation}
\boldsymbol{F}^{\text {bonded}} = \boldsymbol{F}^{\text {bond}} 
                                    + \boldsymbol{F}^{\text {angle}} 
                                    + \boldsymbol{F}^{\text {dihedral}} 
                                    + \boldsymbol{F}^{\text{improper}}
\end{equation}


Molecular Dynamics forces include bonded terms, pairwise bond, angle, and dihedral; and non-bonded terms, van der Waals and Coulomb .the equation of bond force is expressed as

\begin{equation}
\boldsymbol{F}_{i}^\mathrm{LJ}\
=\sum_{j \neq i} \frac{\epsilon_{a b}}{\sigma_{a b}^{2}}\left\{12\left(\frac{\sigma_{a b}}{\left|\mathbf{r}_{j i}\right|}\right)^{14}-6\left(\frac{\sigma_{a b}}{\left|\mathbf{r}_{j i}\right|}\right)^{8}\right\} \mathbf{r}_{j i}
\end{equation}


\begin{equation}
    \boldsymbol{F}_{i}^{\mathrm{Coulomb}}=
        \frac{q_{i}}{4 \pi \epsilon} \sum_{j \neq i}  
        \left\{\frac{1}{\left|\mathbf{r}_{j i}\right|}\right\}^{3}  
        \mathbf{r}_{ji}
\end{equation}


the $\epsilon_{a b}$ and $\sigma_{a b}$ are parameters of particles.the non band force contain Lennard jones and Coulombic forces as equation 6 and 7.


\subsection{Look Up Table}

To improve the computational efficiency, the finite-range force uses the proximity list lookup method, which obtains the spatial relationship between each particle by using the coordinate and distance information for each particle and stores these data into the neighborhood. When affected by the short-range force, the particle only interacts with its surrounding cellular particles. When the distance increases, all particles in the neighborhood are treated as neighbors to update their coordinates and reconstruct the interrelationships in a new step. The construction of the neighbor list is based on each particle that exists only in a specific region. It is assumed that the interaction force between particles is truncated with a radius of Rm . in this approach, each particle contains a list of possible neighboring particles, which serves as an input to go to the next computational cell.


First, the simulation data set is preprocessed, which is realized by the CPU software code. The three-dimensional space where the simulated particles are located in the X-directions, Y-directions and Z-directions is divided into equal-sized squares, and based on the coordinates of the particles, the information of particles and the positions of neighboring squares are recorded in each cell. The preprocessed particle data cells are stored in the form of a data list in the specific storage area of the global HBM in the FPGA. Next, in the proximity force calculation module of the FPGA, we will read each particle data cell sequentially, calculate the distances of all particle pairs in the cell, and select particle pairs within the truncated distances to calculate the proximity force and the finite-range Coulomb force. In addition, each data cell has 26 adjacent data cells in the three-dimensional space, therefore, particle pairs with inter-particle distances less than the cutoff distance between the center data cell and the adjacent data cells are selected and calculated accordingly. According to Newton's third law, half of the interaction forces between particles in the center data cell and its neighboring data cells can be calculated repeatedly. Therefore, to reduce the number of repeated calculations, only the particle pairs of the 13 neighboring data cells in the upper half of the center data cell are filtered and calculated.


\begin{figure*}[ht]
\centering
\includegraphics[angle=0,width=1.0\textwidth]{./figures/Fig1_Neighboursegment.png}
\caption{ The protein structure (a) and cuboid simulated water box (c) were cut into Cells of equal size by Verlet algorithm (d).
	Among them, Home Cell and the surrounding 26 Neighbor cells (e), of which the upper and lower two are divided into "upper hemisphere"(f) and "lower hemisphere"(g).
	(h)According to the Verlet algorithm, the center particle and the surrounding particles can be divided into equal-sized squares according to the values of $Rm$ parameter and $Rcut$ off}
\end{figure*}

\subsection{Verlet Algorithm}

The acceleration of an atom can be obtained after the force is calculated, and then Newton's equations are integrated over the time step to update the atom's next state of motion, commonly used integrators include the Frog Hopper algorithm and the Velocity Verlet algorithm.

Applying the equations of motion it is then possible to calculate the velocity and position of the atom for the next time step based on the position and acceleration at the current moment and the position at the previous moment. The longer the time step, the more physical time can be modeled per calculation. The equations of motion need to be calculated for all particles in the system.
Currently, the Verlet algorithm is used to update the positions of the atoms, and there are several versions of this algorithm, including the original Verlet algorithm, which is different in form but identical in nature.
There are several versions of this algorithm, including the original Verlet method, the “Leap-frog” form, and the “Velocity Verlet” form.



Using the acceleration and position at the moment $t_{0}$, the following formula is obtained:


\begin{equation}
    \mathbf{r}_{i}(t_{0}+\Delta t )= 2\mathbf{r}_{i}(t_{0})-\mathbf{r}_{i}(t_{0}-\Delta t)+ (\Delta t^2/m_{i})f_{i}(t_{0})
\end{equation}


\begin{equation}
    \mathbf{v}_{i}(t_{0})=(\mathbf{r}_{i}(t_{0}+\Delta t )-\mathbf{r}_{i}(t_{0}-\Delta t ))/2\Delta t
\end{equation}


The Verlet algorithm requires a motion update calculation for each particle within the simulated system, and the calculation requires a two-step difference formulation. It needs to utilize the position information of the particle at the previous moment $t_{0}-\Delta t$ and the acceleration and position information at the current moment $t_{0}$. In implementing the algorithm, the position and acceleration of the current moment and the position of the previous moment need to be stored.

\section{Proposed  Architecture}

Combined with the implementation block diagram above, the processing flow of data in the RL module is as follows: the CPU pre-processes the dataset of the MD simulation, and the pre-processed data will contain the following information, in which the cell serial number is obtained by segmenting the original data, and each particle will be assigned to a unique cell, and the particle information will be bound to the cell serial number to form the basic information of the particle to be processed. All other information in the data entry is obtained from the original data file. The side length of each data cell is determined based on the size of the truncation distance, and the number of data cells after CPU pre-processing is determined if the simulation data set exists in a deterministic rectangular space. Each data cell contains information about the particles it belongs to and the positions of the neighboring data cells, forming a data table to be stored in memorizer.


\begin{figure*}[ht]
\centering
\includegraphics[angle=0,width=1.0\textwidth]{./figures/Fig2_top_module_a_b.png}
\caption{Block diagram of the implementation of the finite range force calculation module. Module b, for a single FPGA acceleration card, the user logic resources MD hardware computation is mainly divided into short-range force force, long-range force force and covalent bonding force three kinds of processing units. Module a,The whole RL module is composed in each sub-module of the frame.}
\end{figure*}


Each particle occupies one row of storage space. The length of the data cell is created according to the maximum number of particles after pre-processing, with an appropriate margin. Based on the serial number of the data cell, the starting position of the data cell can be directly located. Excluding particle information in pairs directly creates the structure shown in the following table. The number of excluded particles and their storage addresses are configured in registers for use by the relevant modules. The proximity force calculation requires the use of interpolation and coefficient tables, which are also prepared by the CPU and configured in the RAM of the FPGA. The information of the particle pairs to be excluded mainly comes from the existing co-bonded particle pairs, and the information of the two bonded particles is combined together to form the exclusion information data table, which is stored in the memorizer.

After the above configuration is completed, the CPU starts the prefetching particle data unit module to execute the next operation. The CPU can read the excluded particle information in parallel, and the particle reading module provides the particle pair information to be calculated.

Multi-Channel Calculation Filter Calculation Module, select each data cell as the main data cell in turn, take out the particle information of the main data cell and the particle information of the first 13 related data cells, take out a particle from the main cell, and take out one particle from each of the 13 neighboring cells, and form a particle pair to perform the distance calculation. Another particle is taken out from the main cell and formed into a particle pair with the current particle in the main cell for distance calculation. At this time, the current particle and 14 different particles are performing the distance calculation at the same time, and then based on the square of the distance calculation result and the square of the truncated distance, the particle pair smaller than the truncated distance is selected for the subsequent calculation. The coefficients of the interpolation table are obtained by querying the interpolation table based on the squared distances of the selected particle pairs, and the coefficients are obtained by checking the table based on the particle type information of the particle pairs. The coefficient values are based on the charge information in the FPGA which is needed for the calculation of the electrostatic force and the short-range force. At the end of the table, all the parameters for calculating the limited range force are obtained, and all the information is stored in the FPGA on-chip RAM and passed to the particle distance judgment module for screening, and only the particles within the distance range are allowed to carry out the next step in the force calculation pipeline. The calculation progress of the particles is passed to the proximity force calculation monitoring module, which is used to update the proximity force calculation result data table and supervise the progress of calculator calculation.

The proximity force calculation unit contains several calculation pipelines to match the force requirements of the upper and lower modules. The calculator performs all the calculations of the proximity force and obtains the results of the 3D coordinate axis decomposition based on the finite-range forces of the particle pairs. Since the calculation of the force from the same particle to different particles in each master data cell is performed continuously, the force of the first 13 neighboring data cells can be accumulated and then cached, and the data of these 13 neighboring data cells are cached into the particle pair force cache RAM according to the particle serial number, so as to accumulate all the force data of the particles. All the obtained force results and part of the force accumulation results are cached in the corresponding cache area according to the particle number at the source end. At least two accumulator pipelines and two memory entry generation modules are reserved for parallel processing to match the upstream and downstream processing capabilities.

\begin{figure}[ht]
\centering
\includegraphics[angle=0,width=0.5\textwidth]{./figures/Fig3_HBM.png}
\caption{Data is read and written using HBM memory}
\end{figure}



Module a, the use of HBM multi-module multi interface, HBM for multi-channel read, can support up to 32 channels of data at the same time read and write storage area, data read and write using HBM driver IP, HBM for multi-channel read and write, each HBM supports 32 independent read and write channels, in the design of the internal RAM resources can be saved, the cached data will be stored in the external mass storage device. 
Module 2 is a multi-channel read/write control, HBM2 adopts AXI3 protocol, and AXI3 to AXI4 protocol is used to ensure the consistency of the protocol for FPGA to read/write the external storage device, assuming that 16 read/write channels are used in this figure, and the HBM2 supports up to 32 data read/write channels. 
Module 3 is a multi-channel polling control module, which needs to notify the control module to access the corresponding interface channel after the calculation of multiple RL modules of FPGA, and when a certain channel is occupied, the channel will be prohibited from accessing. 
Module 4 is a module for accumulating multiple HomeCells with the first 13 neighbor cells. 
Module 5 is the computation progress supervision module for multiple short-range force computing modules. Multiple computing modules in a single FPGA logic chip require a secondary computation progress supervision circuit to supervise the Mark table module for each subcell. Module 6 accumulates the results for multiple HomeCells with the remaining 13 neighbor cells.

\section{Memory}

\begin{figure}[ht]
\centering
\includegraphics[angle=0,width=0.4\textwidth]{./figures/Fig4_onecache.png}
\caption{Data cached at the same address, multiple channels read simultaneously}
\end{figure}

\begin{figure}[ht]
\centering
\includegraphics[angle=0,width=0.4\textwidth]{./figures/Fig5_twocache.png}
\caption{Data cached at the different address, multiple channels read simultaneously}
\end{figure}


\section{Data Structure Memory}

\begin{sidewaysfigure}[p]
	\centering
    \includegraphics[angle=0,width=0.9\textwidth]{./figures/Fig6_Upper_half_of_data_structures.png}
	\caption{
        (a) Particle information storage table Home Cell and Neighbor Cell storage relationship.
		(b) The storage relationship between Home Cell and Neighbor Cell in the particle information storage table, the data length is 512 bits, and the data is mainly composed of six parts.
		(c) The data structure of the particle information storage table Home Cell, the data length of the Home Cell is 512 bits bit width, and the non-data part needs to be zeroed in to facilitate the addressing
		(d) Particle pair computational information cache, data bit width is 256 bits bit width, since each Home Cell data is pre-computed with the first 13 Neighbor Cells}
\end{sidewaysfigure}



The data cells are stored in the HBM. The data cell topology in the following figure shows the contents of a data cell. The first row is the index row of the data cell information, including the cell serial number, cell length, number of particles in the cell, offset address of the particle information in the cell, and the check bit. The second and subsequent four rows contain the serial number and the offset address in the HBM of the neighboring cells. This information is arranged in groups of 8 bytes in consecutive rows, and the end of the rows are filled to the whole row using padding data. From the fifth row onwards is the serial number of the particle contained in the cell, which is laid out consecutively, and the end of the row is filled to the end of the row with padding data.

Each particle occupies one row of storage space. The length of the data cell is created according to the maximum number of particles after pre-processing, with an appropriate margin. Based on the serial number of the data cell, the starting position of the data cell can be directly located. Excluding particle information in pairs directly creates the structure shown in the following table. The number of excluded particles and their storage addresses are configured in registers for use by the relevant modules. The proximity force calculation requires the use of interpolation and coefficient tables, which are also prepared by the CPU and configured in the RAM of the FPGA. The information of the particle pairs to be excluded mainly comes from the existing co-bonded particle pairs, and the information of the two bonded particles is combined together to form the exclusion information data table, which is stored in the HBM.

Particle information storage table HomeCell and NeighborCell storage relationship, the data length is 512bits bit width, non-data part needs to make up the zero to it for facilitating addressing and searching, the data is mainly composed of 6 parts, each part of the bit width of the corresponding storage, the starting position for the data type lookup code, followed by the storage relationship between the Cells, the data type A part for the Proofreading part, the user-defined logic needs to get the type of data block according to the identification code, and do the corresponding calculation, the cell serial number is to encode each Cell in the simulation system, to ensure that each Cell has an independent identification code, the cell coordinate is the position of each Cell in the spatial coordinate system, and the cell particle serial number is the particle serial number of each particle in the simulation system.
The particle information is stored in the HomeCell data structure, the data length of HomeCell is 512bits bit width, and the non-data part needs to be complementary zero for it so as to facilitate addressing and searching, the data mainly consists of 6 parts, and the corresponding bit width of each part is reserved for storing, and the starting position is the data type lookup code, and the next one is the particle information. The charge, velocity and coordinate information of each particle is stored in 32bits floating point numbers.

Particle pair computation information is cached in the global memory with a bit width of 256bits. Since each HomeCell is precomputed with the first 13 Neighbor Cells, and there are still 13 Neighbor Cells left to be computed, the computation information of particle pairs needs to be cached in the global memory.

\begin{figure}[ht]
\centering
\includegraphics[angle=0,width=1.0\textwidth]{./figures/Fig7_lower_half_of_data_structures.png}
\caption{(a) The Home Cell and the remaining 13 neighbor cells accumulate the results, and the data needs to be filtered a lot before the particles in the corresponding cell can calculate the final short-range force. 
(b) The block diagram of the storage structure of the RL force result buffer, the output of the calculation result of the particle pair, and the storage length of each particle information is 512 bits.Block diagram of the storage structure of the RL force result buffer, the output of the calculation result of the particle pair, the storage length of each particle information is 256bits, and the calculation result of each step is stored in a specific storage space, which is carried by the CPU for the remaining calculation processing.}
\end{figure}

Particle-to-force caching and partial summation module receives the result of particle-to-force calculation as the force between the same particle and different particles in a main cell in a consecutive clock period, so it can cache this part of the sum of the force first, thus reducing the amount of data to be cached. 
However, this part of the calculation result is also the data needed for other particles to calculate the force, so it needs to be cached and read when calculating the force sum of other particles. 
Therefore, the force is totalized based on the serial number of the source particle, and then the totalized result is cached in the force cache of the source particle. Based on the particle number, all the force results are stored in the force buffer of the host particle, and the RL force buffer is stored consecutively according to the particle number. 

Each particle's force cache is of the same size, and the storage structure is shown in the figure below. 
The first row is the index of the cache information, including the type flag, particle number, maximum cache capacity, valid data number and check digit, etc. 
The second row is the index of the cache information, including the type flag, particle number, maximum cache capacity, valid data number and check digit. 
The second row starts with the cached force vector data, each group of data occupies 16 bytes and is stored continuously. 
There are three types of force vector data in the cache: the first one is the partially summed data, the second one is the force data calculated by the end particles in the particle pair, and the third one is the force data of the excluded particles. The maximum number of force data to be cached in each buffer is set aside as twice the maximum number of particles in the cell obtained during data pre-processing. The number of valid data is initially zero and is increased by one each time data is added. The parity bit is used to check the data in the index line of the cache information for errors and is the parity value of the first 16 bytes. Finite Role The initialization of the finite force buffer can be implemented by software or FPGA logic to restore the information index line of each particle force buffer to its initial value. The number of valid data is zero. Each time data is added, the data add position is calculated based on the number of valid data, and the values of the number of valid data and the parity bit are updated.


\section{Memory}

\begin{figure}[H]
\centering
\includegraphics[angle=0,width=0.5\textwidth]{./figures/Fig8_force_direct_calculation_unit_module.png}
\caption{The block diagram of the direct calculation module, assuming that particle i and particle j have gone through the particle screening module, and the short-range force of the particle pairs needs to be calculated within a certain range of action. The particle pairs are calculated directly by the DSP calculation pipeline, and the calculation results are single-precision floating-point numbers, which are calculated by the Xilinx floating-point number calculation unit, which is a relatively high-precision calculation method that consumes a large amount of DSP resources.}
\end{figure}


Proximity force direct calculation unit module block diagram, assuming that particle i and particle j through the particle screening module, in a certain range of action need to calculate the short-range force of particle pairs, particle pairs through the DSP computing pipeline direct calculation, the calculation result is a single-precision floating-point number, using the Xilinx floating-point number calculation unit, this way of calculating the accuracy is relatively high, the consumption of DSP resources is more. The particles first go through the floating-point subtraction IP output multiplication IP to calculate 2. 
In the hardware floating-point calculation, opening the root sign needs to consume a lot of logic resources, so we calculate the inter-particle interaction force, transform the formula form, and use 2 to do multi-level multiplication to calculate 14 and 8 in place of  13 and 7, which replaces the floating-point computation IP open-root operation. Using the open-root number to handle another Coulomb force calculation pipeline, parallel computation does not interfere with each other in the computation time, the data is output to the last step after floating-point number multiplication and division calculation IP, and the corresponding particle parameters are read from the memory, such as A, B and other parameters in Fig. After the calculation, the results for the LJ force and the Coulomb short-range force.


\begin{figure}[H]
\centering
\includegraphics[angle=0,width=0.5\textwidth]{./figures/Fig9_Third_order_linear_interpolation.png}
\caption{The figure shows the framework diagram of the second-order interpolation table DSP processing flow, the information of each particle is read from the parameter cache, after the arbitration of the parameter selector, the particle pairs go through the distance judgment, and the particles within the computation range enter into the force computation channel, assuming that the particles to be computed are particle i and particle j. }
\end{figure}


The design of the calculation module of the multi-channel calculation screener is realized as follows:
The calculation of the particle pair distance squared and the particle pair screening function is to include the three-dimensional coordinates of the two particles in the information of the particle pair to be calculated, and the distance squared of the particles is equal to the sum of the squares of the differences of the coordinates in the three dimensions.

\begin{equation}
    r_{ij}^2 = ( x_{i} - x_{j} )^2 + ( y_{i} - y_{j} )^2 + ( z_{i} - z_{j} )^2 
\end{equation}

The particle coordinates are represented using single-precision floating-point numbers, and $R^2$ is also stored using single-precision floating-point numbers. The implementation of this formula is realized using floating-point arithmetic IP provided by the Xilinx manufacturer, together with auxiliary circuits to form a computational pipeline, where the distance squared for a set of particles can be computed for each clock. Since 2 has a limited range of values in the analog box and in making distance size comparisons,

coefficient lookup tables, it is more convenient to use integers, all $R^2$ is uniformly enlarged by a certain factor, and then the integer part is taken for particle pair screening. particle pairs for which $R^2$ is smaller than the square of the truncated distance are selected as inputs to the subsequent calculations, otherwise ignored.

Otherwise, they are ignored. Since the spatial center distance of adjacent cells is greater than or equal to the truncation distance, there exists a certain probability distribution for the selected usable particle pairs. To maximize the arithmetic power, we use eight computation and screening modules working in parallel with three ports outputting at the same time, and the related table lookup operation is also allowed to look up the table at the same time according to the number of output ports.


\section{Generation of Interpolation and Coefficient Tables}


\begin{equation}
    F^{RL}/r_{ij} = A_{ab} r_{ij}^{-14} 
                  + B_{ab} r_{ij}^{-8} 
                  + Q_{ab}r_{ij}^{-3}
\end{equation}

 
wthere, 

\begin{equation}
    A_{ab} = 48\epsilon_{ab}\sigma_{ab}^{12}
\end{equation}



\begin{equation}
    B_{ab} =-12\epsilon_{ab}\sigma_{ab}^6
\end{equation}

 
\begin{equation}
    Q_{ab} = \frac{q^a q^b}{4 \pi \varepsilon}
\end{equation}
  
  
In this design, $R^{-14}$, $R^{-8}$ and $R^{-3}$ are used in the formula of proximity force. 
In this design, $R^2$ is used as the output, and all the interpolating functions of the above three functions are created, which are reduced to low-order monic polynomials and then brought into $R^2$ to compute the original values, thus simplifying the computation process and reducing the consumption of DSP resources. 
The generation of the interpolating function should not only reduce the computational complexity, but also ensure that the accuracy of the computation meets the design requirements.

The $R^{-14}$, $R^{-8}$ and $R^{-3}$ can be fitted using a multi-order interpolation function of the form:


\begin{equation}
\begin{array}{c}
 f(x) = C_{0}+C_{1}(x-a)+C_{2}(x-a)^2 +C_{3}(x-a)^3 + \cdots \\ 
 + C_{p}(x-a)^p +o(x^p)
\end{array}
\end{equation}


  After simplification, the accuracy requirement can be met by using second or third order function fitting. The following equation shows the third-order interpolation function form:
  

\begin{equation}
  F(x) = C_{0}+C_{1}x+C_{2}x^2 +C_{3}x^3
\end{equation}


Establish a reasonable interval, the interval sampling points of the formula derivative solution, you can get the corresponding interval of the fitting function. The following formula is a set of equations for the second-order interpolation function to find the value of the interval $[x_{0}, x_{1}]$, for example, can be calculated to find the values of the three coefficients a, b and c:


\begin{equation}
\begin{array}{c}
f(x_{0}) = cx_{0}^2 +bx_{0}+a\\
f^{'} (x_{0}) = 2cx_{0}  +b \\
f(x_{1}) = cx_{1}^2 +bx_{1}+a\\
f^{'} (x_{1}) = 2cx_{1}  +b \\
\end{array}
\end{equation}


Assuming a fit to $R^{-14}$ with $R^{2}$ as the variable, the form of the equation can be simplified to $y( x ) = x^{-7}$.
The $y( x )$ function is monotonically decreasing in the range where x is greater than zero and very steep in the interval x( 0, 1]. The design requires values of $y( x )$ in the range $x [0.1, 144]$.
The greater the number of interpolation points and the smaller the intervals, the higher the computational accuracy, but the greater the number of segmented functions formed between the interpolation points, the larger the capacity of the coefficient table.
In addition, the higher the order of the interpolation function, the stronger the ability to track the function curve changes, the higher the accuracy, the more computing and storage resources consumed.
Therefore, we need to make a compromise according to the actual situation.
We use Python to compare and analyze the order of the interpolation function and the number and distribution of the interpolation points, and choose the second-order polynomial interface to meet the design requirements if the relative error is better than one-thousandth of a degree, and the polynomial library of Numpy provides the coefficients of polynomials fitting functions, which can be directly specified to the required order, and the results are comparable to those of the orthogonal polynomials such as Legendre's. The results are similar to those of the orthogonal polynomials. 
The results are similar to those of orthogonal polynomials such as Legendre. Our final choice is the result of the calculation:

The x interval is divided into 8 segments, as shown in the table, of which the first 7 segments, each segment is divided into 16 small segments, and the last segment, divided into 128 small segments, a total of 240 interpolation functions.
Each interpolation function requires 3 coefficients, expressed as single-precision floating-point numbers.
The interpolation table for each function can be fully stored using a 4KB BRAM.

The calculation of the proximity force also requires a table of coefficients for Aab and Bab, which need to be combined two by two for the particle types, so that if there are N independent particle types, the length of the table will be $N^2$ items. After analyzing the simulation dataset, there are no more than 32 independent particle types, and the depth of the coefficient table is no more than 1024 items. The coefficients are expressed as single-precision floating-point numbers, and the coefficient table can be stored in 8KB BRAM.


\begin{table}[!ht]
    \centering
    \begin{tabular*}{\hsize}{@{}@{\extracolsep{\fill}}llllllll@{}}
   \toprule
    segments & interval, unit:~\AA  \\
   \midrule
    1 & {[}0.1, 0.2{]}  \\  
    2 & {[}0.2, 0.4{]}  \\ 
    3 & {[}0.4, 0.8{]}  \\  
    4 & {[}0.8, 1.6{]}  \\  
    5 & {[}1.6, 3.2{]}  \\ 
    6 & {[}3.2, 6.4{]}  \\  
    7 & {[}6.4, 16.0{]} \\  
    8 & {[}16, 144{]}   \\  
   \bottomrule
    \end{tabular*}
      \caption{The segmentation information of the interpolation table is divided into a total of 8 segments to fit, and the segmentation length of each segment of data is different, in the first interval the data change is steeper, so the segmentation interval is smaller, and then the data change is gradually flattened, and the interval is gradually larger}
\end{table}

The inputs to the third-order interpolating proximity force calculation unit are all the parameters needed for the calculation formula, and the outputs are the three components of the force. Each clock cycle can input a set of calculation parameters, after the internal pipeline processing, each clock can output a set of settlement results. The following figure shows the realization of the proximity force calculation unit block diagram.

\section{Mark Table}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{./figures/Fig10_marktable.png}
	\caption{The particle computing progress monitoring module is composed of several modules}
\end{figure}

The particle computing progress supervision module consists of several modules. 
Module 1 is a two-dimensional matrix, the serial number of the Neighbor Cell corresponding to each Home Cell needs to be cached in the two-dimensional matrix, and the size of the two-dimensional matrix is determined by the Cell number. 
The size of the 2D matrix is determined by the cell number. 
Module 2 Home Cell Calculation Matrix consists of a 1D matrix, which is convenient for data search and comparison.

Module 3 is the comparison circuit, the data is polled by the Home Cell.. After comparing the data in Modules 1 and 2, the Home Cell that meets the conditions will be selected by the comparison module. 
Module 4 is the Mark Table Control Module, which monitors the sequence of each Home Cell data, and consists of several counters in this module. 
Module 5 is the data read/write module, the selected Cell needs to read the corresponding external storage address to access the particle information. 
Module 6 is the data accumulation module, which completes the process of accumulating the selected cell with the remaining 13 neighbor cells.
Module 7 is the external storage device, the particle storage information is large, each Cell and the remaining 13 neighbor cells cumulative process needs to access 13 data blocks. 


\section{Result}

\subsection{High-Level Language Synthesis (HLS) Design Experiment Results}

The HLS compilation tool optimizes DSP resources, and we focus on testing the performance optimization effect of particle-to-computation units, using the HLS synthesis tool for first-order linear interpolation.

Using Xilinx's HLS for the multiply-add implementation of second-order polynomials, the synthesis results in the use of four floating-point IPs (two multipliers and two adders) for a total of 10 DSP48 modules. With a clock constraint of 300MHz, the pipeline optimization requires 22 clock cycles to complete the implementation.
If the clock constraint is 200MHz, it takes 18 clock cycles for Pipeline to complete the function.
To ensure the accuracy of the calculation and consider the range of values of the coefficient table, it is not worthwhile to convert the fixed-point realization.
Regardless of the implementation method, if Xilinx's floating-point computing IP is ultimately used, the performance and resource usage can be expected to be comparable.
The calculation of the square of the distance between two particles consumes approximately 19 DPS48 modules and takes 25 clock cycles.

Each RL module uses approximately one $R^2$ computational unit for particle pair screening, three computational units for interpolating polynomials and one force merger accumulator for force vector computation, and a number of accumulators for accumulating force vectors of neighboring cells.




\begin{table}[!ht]
	\centering
	\begin{tabular*}{\hsize}{@{}@{\extracolsep{\fill}}lllllllllll@{}}
		\toprule
		target & Estimated & Uncertainty  \\ 
		\midrule
		3.33ns & 2.342ns & 0.90ns \\ 
		\bottomrule
	\end{tabular*}
	\caption[Estimation of multiplication-add operation time for second-order polynomials using Xilinx's HLS]{Estimation of multiplication-add operation time for second-order polynomials using Xilinx's HLS with clock constraint of 300MHz}
\end{table}

\begin{table*}[!ht]
	\centering
	\begin{tabular*}{\hsize}{@{}@{\extracolsep{\fill}}llllllll@{}}
		\toprule
		Modules&Latency(cycles) & Latency(ns) & DSP &FF &LUT \\\\ 
		\midrule
	First Order Linear Interpolation Modules 
        &22 &73.304 & 10 & 1459 & 763\\\\
        Second Order Linear Interpolation Module &34 &113.288 & 16 & 1788 & 1063\\\\
        Third Order Linear Interpolation Module &45 &148.514 & 22 & 2442 & 1452\\\\ 
	\bottomrule
	\end{tabular*}
	\caption[Performance and Resource Evaluation]{Performance and resource evaluation of multiplication and addition of first-order polynomials using Xilinx's HLS, totaling 10 DSP48 modules. With a clock constraint of 300MHz, Pipeline requires 22 clock cycles to complete the functional implementation. The performance and resource evaluation of multiply-add second order polynomials using Xilinx's HLS is performed on a total of 16 DSP48 modules. Pipeline requires 34 clock cycles to complete the functional implementation; Xilinx's HLS is used to evaluate the performance and resources of the multiply-add operation for third-order polynomials, totaling 22 DSP48 modules. With a clock constraint of 300MHz, Pipeline requires 45 clock cycles to complete the function}.
\end{table*}


\begin{table*}[!ht]
	\centering
	\begin{tabular*}{\hsize}{@{}@{\extracolsep{\fill}}llllllll@{}}
		\toprule
		Type &Module 2 utilization rate\%&Module 1 utilization rate\%  \\ 
		\midrule
		LUT &10 & 21 \\ 
		LUTRAM & 3 & 6  \\ 
		FF & 13.13&  26.4 \\ 
		BRAM & 7 & 18.5 \\ 
         URAM & 0.63 & $\sim$\\ 
		DSP &2& 4  \\ 
		IO & 1  & 1 \\ 
		GT & 17 & 34   \\ 
		BUFG & 3  & 6 \\ 
		MMCM & 8  & 16 \\ 
		PLL & 17 & 34 \\  
		\bottomrule
	\end{tabular*}
	\caption {(a) shows one Range Limited module layout wiring diagram, (b) shows two Range Limited module layout wiring diagrams}
\end{table*}

\begin{figure}[H]
\centering
\includegraphics[angle=0,width=0.5\textwidth]{./figures/Fig11_result.png}
\caption{(a) shows one RL module layout wiring diagram, (b) shows two RL module layout wiring diagrams}
\end{figure}

 \begin{figure}[H]
	\centering
	\includegraphics[angle=0,width=1.0\textwidth]{./figures/Fig12_fpga_fft_64.png}
	\caption{18,000 Argon  gas particles, using Inspur FPGA accelerator card plus OpenMM software interface for calculation results, data energy calculation results. (a) is the overall Energy Waveform.}
\end{figure}



\section{Conclusions}

\noindent
In MD hardware design, the finite-range interaction force is one of the modules that can best reflect the parallelism of the hardware, this design adopts the classical acceleration algorithms such as the proximity list lookup method, and synthesizes the performance of each expansion module of the FPGA device to make a reasonable design of the module. 
In the process of particle-pair interaction force calculation, a large amount of intermediate computational data will be generated, which need to be reasonably spliced and cached, relying only on chip memory is far from enough.\\

\noindent
In this chapter, the basic definition, main characteristics and development history of FPGA are summarized, and compared and analyzed with general-purpose processors. 
An all-hardware module is designed to accelerate the calculation of molecular dynamics, which is aimed at increasing the calculation speed, and the pipeline technique is used to optimize the allocation of tasks in the whole parallel system. 
The Single Instruction Multiple Data Stream (SIMD) approach is utilized to significantly improve the computational efficiency. At the same time, we have built a software framework dedicated to this scheme, which can be used to accelerate the data processing of the circuit.\\

\noindent
To better exploit its parallel potential, we comprehensively compare existing high-speed interfaces and memory devices, and comparatively analyze existing high-speed architectural designs.
The new generation of high-bandwidth memories, such as HBM2 memories, are used to fully compare the impact of existing memory structures on computational performance in MD design.
A lot of work has been done using DDR4 high-speed interface, and the computation uses polling algorithm to improve the access efficiency.
The new generation of high-speed and high-bandwidth multi-parallel interface HBM memory is adopted, so that the read and write access to external mass storage can be completed while the parallel computation of multi-path data. 
The optimized design of the underlying RTL circuits fundamentally improves the efficiency of data read and write registers, and more than doubles the efficiency of reading and writing external storage. Therefore, the rational use of external mass memory devices is fully considered in the design process. 
We selected the same cloud platform Xilinx UV9P chip, in the design of the DDR4 and HBM2 two relatively efficient memory, because in the MD calculation there are a large number of intermediate computational data, which write data directly related to the particle information, so the storage of data need to be stored in an independent storage interval, the DDR is a single read-write channel, the HBM support for 32 independent read-write channels, which for the Particle computing read/write storage space greatly improves the data throughput efficiency.
The use of multi-channel computation improves the efficiency of particle reading and writing to external memory, but the consumption of on-chip resources by the read/write driver needs to be fully considered. We adopt the progress proofreading module to supervise the multi-channel data to ensure the orderly reading and writing of data.\\

\noindent
For the same MD simulation system, the data are "sliced" by matching the computational nodes and sent to the designated computational unit.
The computation unit needs to adopt a reasonable floating-point computation mode. In this design, we compare a variety of numerical computation models, using multi-order linear interpolation, respectively, for the first-order, second-order, third-order computation models, each section of the fitted curve to do a reasonable cut modeling, such as the above $X ^ {-7} $, to build the numerical interpolation table. 
The interpolation table is transferred to the on-chip memory via high-speed AXI lite protocol, which ensures high computational efficiency. 
At the same time, HLS advanced compilation means and RTL design means are used, and finally RTL design circuits are used to ensure computational efficiency. 
The interpolation algorithms are compared with Newtonian interpolation, Hamiltonian interpolation and Legendre interpolation.\\

\noindent
After the same MD simulation system has been divided into chunks, it is necessary to design a reasonable proximity force computation supervision module, which needs to record the computational progress of each module data. 
Assuming that a simulation system is divided into 1000 or more sub-Cells, and at the same time, the means of separate computation in the upper and lower hemispheres are used, which undoubtedly increases the difficulty of designing the supervision module. 
We adopt a multi-level supervision means to ensure that the computation is carried out efficiently.\\


\clearpage
%
% Reference
%

\bibliographystyle{unsrt}
\bibliography{main}


\end{document}